services:
  consumet:
    image: ghcr.io/consumet/api:latest
    container_name: anime-app-consumet
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
